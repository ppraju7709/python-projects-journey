# Factorial Calculator 🔢

## Description
This project is a Python program that calculates the **factorial** of a
given non-negative integer.

The factorial of a number `n` is the product of all positive integers
from `1` to `n` and is denoted as `n!`.

Example:
- `5! = 5 × 4 × 3 × 2 × 1 = 120`

---

## Features
- Calculates factorial of a given number
- Handles edge cases like `0`
- Clear and readable output
- Simple and beginner-friendly logic

---

## Concepts Used
- Variables and Data Types
- User Input and Output
- Conditional Statements (`if-else`)
- Loops (`for` or `while`)
- Basic mathematical logic

---

## How to Run
This project can be run locally using Python.

```bash
python main.py
